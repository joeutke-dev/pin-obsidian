{
	"name": "float-on-top",
	"version": "1.0.4",
	"description": "Keep the window above other windows, with adjustable translucency.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"always-on-top",
		"opacity",
		"window"
	],
	"author": "Joe Utke",
	"license": "MIT",
	"devDependencies": {
		"@electron/remote": "^2.1.3",
		"@types/node": "^20.11.0",
		"electron": "^39.8.10",
		"esbuild": "^0.21.5",
		"obsidian": "latest",
		"tslib": "^2.6.2",
		"typescript": "^5.4.5"
	}
}
